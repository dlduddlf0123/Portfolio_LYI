using System.Collections;
using System.Collections.Generic;
using UnityEngine;


namespace Burbird
{


    public class Backend_LoginManager : MonoBehaviour
    {
        BackendManager backendMgr;

        private void Awake()
        {
            backendMgr = GameManager.Instance.backendMgr;

        }

        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}